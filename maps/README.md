# Competition map

The team’s saved arena map for localization and navigation lives here as `restaurant.yaml` and `restaurant.pgm`. Use this map on competition day — do not rebuild it during a run unless the venue layout has changed and the team agrees to update it beforehand.

See [docs/navigation.md](../docs/navigation.md) for how to load and use the map.
